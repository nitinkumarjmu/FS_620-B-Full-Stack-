# Todo Client (React + Vite)

## Setup
1. cd client
2. npm install
3. npm run start

The client expects the backend API to be available at `/api/todos`. For local development, consider running the server on port 5000 and use a proxy in vite.config or run both with appropriate CORS configuration.
